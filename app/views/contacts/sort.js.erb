$("div#results").html("<%= escape_javascript(render :partial => 'list', locals: {contacts: @contacts}) %>");
add_activity("Contacts sorted by <%= @order.gsub('_',' ').titleize.gsub('Desc','').strip %> <%= @order.include?('DESC') ? '(Z-A)' : '(A-Z)' %>")