!!!
%html
  %head
    %title Noticery
    %link{:href => "/noticery_2.ico", :rel => "shortcut icon"}
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %div.container-notification
      - if Rails.env == 'development'
        - if notice.present?
          %div.notice= notice
        - elsif alert.present?
          %div.alert= alert
    %div.title
      = "Noticery"
      %div.subtitle= "The art of BULK messaging"
    - if Rails.env == 'development'
      = yield
      - if current_user.present?
        %br/
        = link_to "Sign Out", "/logout"
    - else
      %div.error
        = "Wait for it."
        %div.note
          = "- Coming Soon -"